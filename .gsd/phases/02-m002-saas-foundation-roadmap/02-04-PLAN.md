# S04: White-label & Custom Domains

**Goal:** Org admins can upload a logo, set a primary color, and configure a custom domain. Middleware forwards host context so API routes can resolve orgs by custom domain.
**Demo:** Logo upload (Vercel Blob), primary + secondary brand colors (CSS vars), custom domain field, UpgradeWall, trial expiry enforcement (client + server)

## Must-Haves

- Complete the planned slice outcomes.

## Verification

- Run the task and slice verification checks for this slice.

<tasks>
- [x] **T01**: Logo upload API (Vercel Blob)
- [x] **T02**: Custom domain field — settings UI + API
- [x] **T03**: Middleware forwards x-org-host
</tasks>
