# S02: Signup Consent Audit Trail

**Goal:** Store privacyConsentAt and consentIp on User at signup for LFPDPPP audit trail
**Demo:** New signup records privacyConsentAt + consentIp on the user row; visible in admin org detail

## Must-Haves

- Complete the planned slice outcomes.

## Verification

- Run the task and slice verification checks for this slice.

<tasks>
- [x] **T01**: Added privacyConsentAt and consentIp to User schema and applied migration _(15m)_
  Add privacyConsentAt DateTime? and consentIp String? to User model; generate and apply migration
  - Files: `prisma/schema.prisma`
  - Verify: docker exec hittek-crm-postgres psql -U crm -d crm_db -t -A -c "SELECT column_name FROM information_schema.columns WHERE table_name='User' AND column_name IN ('privacyConsentAt','consentIp');" | grep -c consentIp
- [x] **T02**: privacyConsentAt and consentIp columns created and populated on signup _(15m)_
  In signup API, capture req headers for IP (x-forwarded-for fallback req.socket.remoteAddress), save privacyConsentAt: new Date() and consentIp to the user create data
  - Files: `pages/api/auth/signup.js`
  - Verify: node --check pages/api/auth/signup.js
</tasks>

## Files Likely Touched

- prisma/schema.prisma
- pages/api/auth/signup.js
