# S01: Admin Panel Completions

**Goal:** Extend admin panel with org creation form, client-side search/filter, and chatbot + conversation counts per org
**Demo:** Admin can create a new org, search existing orgs, and see chatbot + conversation counts per org

## Must-Haves

- Complete the planned slice outcomes.

## Verification

- Run the task and slice verification checks for this slice.

<tasks>
- [x] **T01**: Added chatbots and conversations to _count select in listOrgs _(20m)_
  1. In listOrgs: add chatbots and conversations to _count select
  2. Verify createOrg already works (it does); no changes needed there
  3. Add DELETE action to updateOrg for hard-delete (optional, guarded by confirm)
  - Files: `pages/api/admin/orgs.js`
  - Verify: node --check pages/api/admin/orgs.js
- [x] **T02**: Admin UI: search, create-org form, chatbot+conv counts _(45m)_
  1. Add client-side search input filtering orgs by name/slug
  2. Add chatbots and conversations columns to the table
  3. Add a Create Org expandable form (name field + submit) at the top
  4. Wire form to POST /api/admin/orgs and prepend result to list
  - Files: `pages/admin.js`
  - Verify: node --check pages/admin.js
</tasks>

## Files Likely Touched

- pages/api/admin/orgs.js
- pages/admin.js
