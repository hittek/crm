# S01: Report data models and registry skeleton — UAT

**Milestone:** M010-sewkw9
**Written:** 2026-07-28T16:41:01.375Z

# S01 UAT — Report Framework\n\n## Checks\n\n- [x] `ReportDefinition` table exists with correct columns and FK constraints\n- [x] `ReportRun` table exists with UNIQUE(reportId, organizationId, windowKey)\n- [x] Seed: 2 built-in rows (one per org), isBuiltIn=true\n- [x] `POST /api/internal/run-report` without secret → 403 Forbidden\n- [x] `POST /api/internal/run-report` with correct secret + reportId:1 → already_delivered (idempotency)\n- [x] `POST /api/internal/run-report` with correct secret + reportId:2 → delivered, widgetCount:6\n- [x] No `orgId` column remains in any DB table (all renamed to organizationId)\n- [x] App health check passes after deploy"
