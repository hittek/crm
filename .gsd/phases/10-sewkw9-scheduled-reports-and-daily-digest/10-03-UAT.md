# S03: Query executor and run endpoint — UAT

**Milestone:** M010-sewkw9
**Written:** 2026-07-28T17:04:22.799Z

# S03 UAT — Query Executor\n\n## Checks\n- [x] GET /api/reports/1/run without session → 401\n- [x] GET /api/reports/1/run with session → { widgets: [6 items] }\n- [x] number_card widgets return { data: { value: number } }\n- [x] bar_chart widgets return { data: [{ name, value }] }\n- [x] table widget returns { data: { columns: [...], rows: [...] } }\n- [x] groupBy (stage) produces correct grouped values from live DB\n- [x] Time template {{startOf:month}} resolves in filter values\n- [x] POST /api/reports/preview/run with inline definition returns correct count\n- [x] Custom Claude-built report (id=3) executes in <30ms with 4 widgets\n- [x] App health OK after deploy"
