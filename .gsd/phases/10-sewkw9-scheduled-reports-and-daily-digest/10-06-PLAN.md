# S06: Reports management UI and schedule settings

**Goal:** Reports management UI: tabbed page with Dashboard (existing), My Reports list, AI Builder, and run history viewer using Recharts.
**Demo:** User types 'contactos nuevos por ciudad esta semana', sees bar chart preview appear in ~4s, clicks Guardar, sets schedule to 'Lunes 8am', saves. Next Monday a push notification arrives with the report link.

## Must-Haves

- Reports page has tabs: Dashboard (existing), Mis Reportes (list+run), Crear Reporte (AI builder). AI builder sends prompt, shows live Recharts preview. Saved reports can be run and deleted. Schedule field saved per report.

## Proof Level

- This slice proves: Browser visual

## Integration Closure

New components call /api/reports (list), /api/reports/build, /api/reports/[id]/run, /api/reports/[id] (delete/patch). pages/reports.js gains tab navigation. Existing Dashboard.js preserved as-is in its tab.

## Verification

- None beyond what API endpoints already log.

<tasks>
- [x] **T01**: CRUD and history API endpoints for reports already implemented _(25m)_
  Add GET /api/reports/index.js listing all org ReportDefinitions. Add PATCH+DELETE /api/reports/[id]/index.js. Write GET /api/reports/[id]/history.js returning last 10 ReportRun rows.
  - Files: `pages/api/reports/index.js`, `pages/api/reports/[id]/index.js`, `pages/api/reports/[id]/history.js`
  - Verify: node --check all three files && curl /api/reports with session returns array
- [x] **T02**: ReportRunner Recharts renderer already implemented _(30m)_
  Write components/reports/ReportRunner.js. Accepts widgets array from /api/reports/[id]/run. Renders BarChart, LineChart, PieChart via Recharts ResponsiveContainer, number_card as big stat, table as HTML table. Shows spinner while loading, error chip if widget.error set.
  - Files: `components/reports/ReportRunner.js`
  - Verify: node --check components/reports/ReportRunner.js
- [x] **T03**: ReportBuilder AI prompt UI already implemented _(25m)_
  Write components/reports/ReportBuilder.js. Textarea for prompt, Build button calling POST /api/reports/build then POST /api/reports/preview/run, ReportRunner preview, Save button (save=true), token+widget count display.
  - Files: `components/reports/ReportBuilder.js`
  - Verify: node --check components/reports/ReportBuilder.js
- [x] **T04**: Tabbed reports page already implemented with all i18n keys _(25m)_
  Rewrite pages/reports.js with three tabs: Dashboard (existing), Mis Reportes (list cards with Run/Delete/schedule badge), Crear Reporte (ReportBuilder). Add i18n keys to es.js and en.js.
  - Files: `pages/reports.js`, `lib/i18n/translations/es.js`, `lib/i18n/translations/en.js`
  - Verify: node --check pages/reports.js
- [x] **T05**: Browser verified: 3 tabs render correctly, Recharts charts visible, Mis Reportes shows report cards _(15m)_
  Build, deploy, and verify in browser. Check tabs switch, Dashboard renders, AI builder creates and previews widgets with Recharts, Save creates row visible in list.
  - Verify: browser screenshot shows Recharts charts on /reports
</tasks>

## Files Likely Touched

- pages/api/reports/index.js
- pages/api/reports/[id]/index.js
- pages/api/reports/[id]/history.js
- components/reports/ReportRunner.js
- components/reports/ReportBuilder.js
- pages/reports.js
- lib/i18n/translations/es.js
- lib/i18n/translations/en.js
