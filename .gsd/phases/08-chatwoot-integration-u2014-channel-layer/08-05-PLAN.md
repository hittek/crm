# S05: Conversations Page — Chatwoot-backed Messages

**Goal:** 
**Demo:** Open /conversations → click conversation → full message thread from Chatwoot. Agent reply delivered via WhatsApp. Chatwoot inbox link in sidebar.

## Must-Haves

- CRM Message table removed. All message reads proxy to Chatwoot. Agent replies POST to Chatwoot Messages API. Chatwoot inbox linked in sidebar navigation.

## Proof Level

- This slice proves: Message thread renders from Chatwoot API. Agent reply delivered. Chatwoot inbox link works.

## Integration Closure

Prisma migration removes Message model, adds chatwootConversationId to Conversation. /api/conversations/[id]/messages proxies Chatwoot. Chatwoot inbox URL in Layout sidebar.

## Verification

- Chatwoot API proxy latency logged. Missing chatwootConversationId surfaces as 404 with context.

<tasks>
- [x] **T01**: Agent replies for chatwoot-channel conversations now routed to Chatwoot API _(30m)_
  Parse chatwootAccountId and chatwootConvId from Conversation.sessionId (format chatwoot-{accountId}-{convId}). Update POST /api/conversations/[id]/messages handler: when channel=chatwoot, use sendChatwootMessage to deliver agent reply using org.chatwootAgentBotToken. Add chatwootConvUrl to GET /api/conversations/[id] response when channel=chatwoot.
  - Files: `pages/api/conversations/[id]/messages.js`, `pages/api/conversations/[id]/index.js`, `lib/chatwoot.js`
  - Verify: node --input-type=module < pages/api/conversations/\[id\]/messages.js 2>&1 | grep SyntaxError | wc -l
- [x] **T02**: Conversations UI shows violet Chatwoot badge and link button for chatwoot-channel convs _(20m)_
  Update conversations.js UI: add chatwoot to CHANNEL_META with purple badge. Show Chatwoot link button in conversation header when channel=chatwoot (links to chatwoot.hittek.mx/app/accounts/{id}/conversations/{id}). Add Chatwoot icon link in Layout.js sidebar nav.
  - Files: `pages/conversations.js`, `components/layout/Layout.js`
  - Verify: grep -c 'chatwoot' pages/conversations.js
- [x] **T03**: Agent reply routes through Chatwoot — confirmed in DB with message_id=11 _(20m)_
  Rebuild app container with updated code. Deploy via docker compose up -d app. Verify by posting an agent reply via the API to a chatwoot-channel conversation and confirming it appears in Chatwoot.
  - Verify: docker compose ps | grep crm-app | grep healthy
</tasks>

## Files Likely Touched

- pages/api/conversations/[id]/messages.js
- pages/api/conversations/[id]/index.js
- lib/chatwoot.js
- pages/conversations.js
- components/layout/Layout.js
