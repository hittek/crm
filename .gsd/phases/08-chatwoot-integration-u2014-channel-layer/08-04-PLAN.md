# S04: OAuth Channel Connect UI

**Goal:** Migrate channel connect from raw webhooks to Chatwoot-backed inboxes
**Demo:** Connect WhatsApp Business via one OAuth click. Channel active in under 60 seconds. No tokens pasted.

## Must-Haves

- Connecting Telegram via CRM UI creates a Chatwoot inbox linked to AgentBot; chatwootInboxId stored on ChannelConfig; disconnect removes inbox; Telegram message → Claude reply in Chatwoot

## Proof Level

- This slice proves: integration

## Integration Closure

Telegram inbox created in Chatwoot is linked to the org AgentBot. When a message arrives, Chatwoot fires the AgentBot webhook to the S03 handler. Round-trip verified.

## Verification

- Channel connect/disconnect logged with inbox IDs. Chatwoot API errors logged and surfaced to UI.

<tasks>
- [x] **T01**: Schema fields added, 5 orgs backfilled with admin tokens, inbox helpers in lib/chatwoot.js _(25m)_
  Add chatwootInboxId Int? to ChannelConfig and chatwootAdminToken String? to Organization via ALTER TABLE. Backfill admin tokens for 5 orgs. Add createChatwootInbox, deleteInbox helpers to lib/chatwoot.js.
  - Files: `prisma/schema.prisma`, `lib/chatwoot.js`
  - Verify: node -e "const c=require('./lib/chatwoot'); console.log(typeof c.createChatwootInbox, typeof c.deleteInbox)"
- [x] **T02**: Channel API migrated to Chatwoot inboxes for Telegram connect/disconnect _(30m)_
  Update POST /api/chatbot/bots/[id]/channels for telegram: call createChatwootInbox (creates Chatwoot Telegram inbox + links AgentBot), store chatwootInboxId. DELETE: call Chatwoot deleteInbox. WhatsApp: keep existing path.
  - Files: `pages/api/chatbot/bots/[id]/channels/index.js`, `lib/chatwoot.js`
  - Verify: node --input-type=module < pages/api/chatbot/bots/\[id\]/channels/index.js 2>&1 | grep SyntaxError | wc -l
- [x] **T03**: Channel UI updated and deployed — Telegram connect is now fully automatic via Chatwoot _(25m)_
  Update ChannelsModal in pages/chatbots.js: remove manual webhook copy step for Telegram, add Chatwoot inbox link in connected card. Rebuild and deploy.
  - Files: `pages/chatbots.js`
  - Verify: docker compose ps | grep crm-app | grep healthy
- [x] **T04**: E2E verified — inbox create/delete works, Claude responds to messages through Chatwoot _(20m)_
  End-to-end test: connect a Telegram bot via CRM UI, verify Chatwoot inbox created and linked. Send message, verify Claude reply appears in Chatwoot.
  - Verify: docker logs hittek-crm-app --since 5m | grep agentbot | grep replied | wc -l
</tasks>

## Files Likely Touched

- prisma/schema.prisma
- lib/chatwoot.js
- pages/api/chatbot/bots/[id]/channels/index.js
- pages/chatbots.js
