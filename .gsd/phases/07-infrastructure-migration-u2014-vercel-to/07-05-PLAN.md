# S05: Production Hardening + Smoke Test

**Goal:** Verify the full CRM is production-ready on crm.hittek.mx — login works, core features function, restart policy is correct.
**Demo:** Full CRM walkthrough on crm.hittek.mx: login → contact → deal → Telegram chatbot message → agent reply

## Must-Haves

- Login to crm.hittek.mx succeeds, contacts/deals pages load, no Vercel env vars remain active, containers have restart:unless-stopped.

## Proof Level

- This slice proves: manual — browser walkthrough + curl checks

## Integration Closure

App survives server reboots via restart:unless-stopped, login and core CRM routes work, no Vercel dependencies remain in .env.

## Verification

- None additional.

<tasks>
- [x] **T01**: No Vercel env vars found — .env is clean for self-hosted _(10min)_
  Check .env for any remaining Vercel-specific vars (VERCEL_URL, VERCEL_ENV, etc.) and remove or neutralize them. Confirm DATABASE_URL, NEXTAUTH_URL/SESSION_SECRET, and all required vars are set correctly for self-hosted. Verify no pages hardcode vercel.app URLs.
  - Files: `.env`
  - Verify: grep -i vercel .env | grep -v '#' | grep -v NGROK || echo 'clean'
- [x] **T02**: Smoke test passed — login and health endpoints live on crm.hittek.mx _(15min)_
  1. Open https://crm.hittek.mx in browser (or curl the login page). 2. POST login with seed credentials. 3. Navigate to /contacts, /deals, /tasks — confirm pages load with data. 4. Confirm docker compose ps shows all containers healthy.
  - Verify: curl -sf https://crm.hittek.mx | grep -i 'login\|crm\|hittek'
</tasks>

## Files Likely Touched

- .env
