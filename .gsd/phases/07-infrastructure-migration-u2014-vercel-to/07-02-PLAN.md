# S02: S02

**Goal:** Migrate all data from Neon PostgreSQL to the local Docker PostgreSQL container so the self-hosted app has full production data.
**Demo:** Login with existing credentials — contacts, deals, tasks all visible with migrated data

## Must-Haves

- Login with existing production credentials works. Contacts, deals, tasks, and conversations are all visible. Vector embeddings intact (chatbot RAG functional). Row counts match Neon source.

## Proof Level

- This slice proves: manual — login + spot-check counts in app UI + pg row count comparison

## Integration Closure

App at localhost:3000 uses local postgres exclusively — no DATABASE_URL pointing at Neon remains in .env

## Verification

- None new — /api/health already covers DB connectivity

<tasks>
- [x] **T01**: Dump production data from Neon _(20min)_
  1. Confirm Neon DATABASE_URL is available in .env (or prompt for it).
  2. Run pg_dump with --no-owner --no-acl --clean --if-exists flags to produce a portable dump.
  3. Use pg_dump from a postgres:15 Docker container (matching Neon's pg15) so pg_dump version matches — avoids version-mismatch warnings.
  4. Save dump to neon_dump.sql (gitignored).
  5. Verify dump file size is non-zero and contains expected table names.
  - Files: `neon_dump.sql`, `.gitignore`
  - Verify: grep -c 'CREATE TABLE' neon_dump.sql && wc -l neon_dump.sql
- [x] **T02**: Restore dump into local Docker postgres _(20min)_
  1. Ensure postgres container is running (docker compose up -d postgres).
  2. Copy neon_dump.sql into the container: docker cp neon_dump.sql hittek-crm-postgres:/tmp/.
  3. Run psql restore: docker exec -i hittek-crm-postgres psql -U crm -d crm_db -f /tmp/neon_dump.sql.
  4. If restore errors on extension (pgvector), it's already created by docker-entrypoint-initdb.d — ignore that error only.
  5. Run prisma db push after restore to ensure any schema deltas are applied.
  - Files: `docker-compose.yaml`
  - Verify: docker exec hittek-crm-postgres psql -U crm -d crm_db -c '\dt' && docker exec hittek-crm-postgres psql -U crm -d crm_db -c 'SELECT COUNT(*) FROM "Contact";'
- [x] **T03**: Validate data integrity and app login _(15min)_
  1. Start the full stack: docker compose up -d.
  2. Wait for /api/health to return {status:ok,db:ok}.
  3. Compare row counts across key tables between Neon and local: Contact, Deal, Task, Conversation, Message, User, Organization.
  4. Open localhost:3000 in browser, log in with production credentials.
  5. Spot-check: contacts list loads, at least one deal visible, chatbot knowledge bases intact.
  6. Confirm .env DATABASE_URL points to postgres container (not Neon) — no Neon URL anywhere in active config.
  - Files: `.env`
  - Verify: curl -s http://localhost:3000/api/health | grep '"status":"ok"'
</tasks>

## Files Likely Touched

- neon_dump.sql
- .gitignore
- docker-compose.yaml
- .env
