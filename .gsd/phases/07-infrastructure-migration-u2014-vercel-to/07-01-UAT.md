# S01: Dockerize the Next.js App — UAT

**Milestone:** M007
**Written:** 2026-07-16T21:13:10.610Z

# S01 UAT — Dockerize the Next.js App

## Test Environment
- RPi4 (linux/arm64)
- Docker 24+

## Test Cases

### TC01 — Docker build succeeds on ARM64
```
docker build --platform linux/arm64 -t hittek-crm:test .
```
**Expected:** Exit 0. Image created. ✅

### TC02 — Image size is reasonable
```
docker images hittek-crm:latest
```
**Expected:** < 400MB. **Actual: 231MB** ✅

### TC03 — All services start and reach healthy state
```
docker compose up postgres redis app -d
docker ps
```
**Expected:** All services Up, postgres/redis healthy.

### TC04 — /api/health returns 200 with DB ok
```
curl http://localhost:3000/api/health
```
**Expected:** `{"status":"ok","db":"ok","timestamp":"..."}` ✅

### TC05 — Vercel dependencies are abstracted
- Uploads go to /data/uploads volume when BLOB_READ_WRITE_TOKEN is unset
- @vercel/functions/waitUntil replaced with plain Promise
- next.config.js has output: standalone
