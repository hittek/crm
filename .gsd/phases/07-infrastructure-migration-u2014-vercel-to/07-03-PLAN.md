# S03: S03

**Goal:** Configure ngrok static domain so the app is reachable at a fixed HTTPS URL that survives docker compose restarts.
**Demo:** curl https://crm-hittek.ngrok.app/api/health returns 200 after docker compose restart — same URL every time

## Must-Haves

- curl https://&lt;static-domain&gt;/api/health returns {status:ok} after a docker compose down + up cycle — same URL both times.

## Proof Level

- This slice proves: manual — curl from this machine

## Integration Closure

ngrok container healthy, static URL resolves to app, NEXT_PUBLIC_APP_URL and NEXT_PUBLIC_BASE_URL set to that URL.

## Verification

- ngrok inspection UI available at localhost:4043

<tasks>
- [x] **T01**: Set ngrok credentials in .env _(5min)_
  Collect NGROK_AUTHTOKEN, NGROK_DOMAIN, NEXT_PUBLIC_APP_URL, and NEXT_PUBLIC_BASE_URL. Update .env with real values. Verify docker-compose.yaml ngrok service references them correctly.
  - Files: `.env`
  - Verify: grep NGROK_AUTHTOKEN .env | grep -v placeholder
- [x] **T02**: Verified static ngrok domain survives docker compose down + up cycle _(10min)_
  1. docker compose up -d (full stack). 2. Wait for ngrok container healthy/running. 3. curl https://$NGROK_DOMAIN/api/health. 4. docker compose down && docker compose up -d. 5. curl same URL again — must return same result to prove static domain persists.
  - Verify: curl -sf https://$NGROK_DOMAIN/api/health | grep 'status.*ok'
</tasks>

## Files Likely Touched

- .env
