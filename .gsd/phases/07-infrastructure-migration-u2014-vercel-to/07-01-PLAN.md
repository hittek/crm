# S01: S01

**Goal:** Produce a working multi-stage Dockerfile for the Next.js app targeting linux/arm64. App starts, connects to a local PostgreSQL container, and serves pages. /api/health endpoint added.
**Demo:** docker compose up → app responds at localhost:3000, /api/auth/me returns 200

## Must-Haves

- App container starts cleanly on ARM64. Prisma migrations run on startup. No Vercel-specific build assumptions. /api/health returns 200.

## Proof Level

- This slice proves: docker compose up + curl /api/health

## Integration Closure

Dockerfile + docker-compose.yml (app + postgres + redis) committed. .env.example updated.

## Verification

- /api/health endpoint returns {status: ok, db: ok}. Container logs to stdout.

<tasks>
- [x] **T01**: Audit Vercel-specific dependencies and ARM64 blockers _(30m)_
  Read package.json, next.config.js, prisma/schema.prisma, and lib/prisma.js to identify: (1) Vercel/Neon-specific packages (@vercel/postgres, @prisma/adapter-neon, etc.), (2) native modules that need ARM64 binaries (bcrypt, sharp, canvas, etc.), (3) any NEXT_PUBLIC_ env vars hardcoded or referencing Vercel URLs, (4) build-time env var requirements. Produce a written audit.
  - Files: `package.json`, `next.config.js`, `prisma/schema.prisma`, `lib/prisma.js`, `pages/_app.js`
  - Verify: Audit complete — all blockers listed with resolution plan
- [x] **T02**: Write Dockerfile (multi-stage, linux/arm64) _(1h)_
  Write a multi-stage Dockerfile: (1) deps stage: install node_modules with pnpm, (2) builder stage: run next build, (3) runner stage: copy .next/standalone + static assets, run as non-root user. Use node:20-alpine as base (multi-arch, includes ARM64). Remove any Neon/Vercel adapter — switch prisma.js to standard DATABASE_URL. Add an entrypoint script that runs `prisma migrate deploy` before starting the app.
  - Files: `Dockerfile`, `lib/prisma.js`, `package.json`
  - Verify: docker build --platform linux/arm64 -t hittek-crm:test . exits 0
- [x] **T03**: Write docker-compose.yml (app + postgres + redis) _(30m)_
  Write docker-compose.yml with three services: (1) app: hittek-crm image, env_file: .env, depends_on postgres + redis, restart: unless-stopped; (2) postgres: postgres:15-alpine, volume for data persistence, POSTGRES_DB/USER/PASSWORD from env; (3) redis: redis:7-alpine, volume for persistence. All on a shared bridge network. Health checks on postgres and redis. App waits for postgres healthy before starting.
  - Files: `docker-compose.yml`, `.env.example`
  - Verify: docker compose config validates without errors
- [x] **T04**: Add /api/health endpoint _(20m)_
  Create pages/api/health.js. Returns {status: 'ok', db: 'ok', timestamp: ISO} on success. Checks DB by running prisma.$queryRaw`SELECT 1`. Returns {status: 'error', db: 'error', error: message} with HTTP 503 on DB failure. No auth required.
  - Files: `pages/api/health.js`
  - Verify: curl localhost:3000/api/health returns {status:'ok'}
- [x] **T05**: Smoke test full stack locally _(45m)_
  Run docker compose up -d on the RPi4. Wait for all services healthy. curl /api/health. Verify login works. Check container logs for errors. Fix any startup issues discovered.
  - Files: `docker-compose.yml`, `Dockerfile`
  - Verify: curl https://localhost:3000/api/health returns {status:'ok', db:'ok'}. docker compose ps shows all services Up.
</tasks>

## Files Likely Touched

- package.json
- next.config.js
- prisma/schema.prisma
- lib/prisma.js
- pages/_app.js
- Dockerfile
- docker-compose.yml
- .env.example
- pages/api/health.js
