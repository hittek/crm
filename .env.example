DATABASE_URL="file:./dev.db"

# Session secret for authentication (MUST be at least 32 characters)
# Generate a secure secret for production: openssl rand -base64 32
SESSION_SECRET="your-secure-session-secret-at-least-32-characters"
